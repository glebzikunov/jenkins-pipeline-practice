pipeline {
  agent any 

  stages {
    stage('Setting up the environment') {
      steps {
        echo 'Installing dependencies'
      }
    }

    stage('Running tests') {
      steps {
        echo 'Running test using STRING parameter'
      }
    }
  }
}
